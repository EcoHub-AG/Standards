{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "description": "Payroll split by gender with an optional total",
  "type": "object",
  "properties": {
    "male": {
      "description": "Total wages for men. If available, the definitively settled insured total wages for men in the corresponding calendar year, rounded to the nearest whole CHF. Otherwise, the advance total wages (usually in the current year), rounded to the nearest whole CHF.",
      "type": "integer"
    },
    "female": {
      "description": "Total wages for women. If available, definitively settled insured total wages for women, rounded to whole CHF. Otherwise, advance total wages (usually in the current year), rounded to whole CHF.",
      "type": "integer"
    },
    "total": {
      "description": "Total wages (men and women). This field can be used if it is not possible to divide the wage sum between men and women; otherwise, sum of male and female fields.",
      "type": "integer"
    }
  },
  "required": [
    "male",
    "female",
    "total"
  ],
  "additionalProperties": false
}
